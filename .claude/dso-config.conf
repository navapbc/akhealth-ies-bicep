# DSO configuration for akhealth-ies-bicep
# dso-version: 1.16.11
# Generated by /dso:onboarding on 2026-05-18
# Format: flat KEY=VALUE — see DSO CONFIGURATION-REFERENCE.md

# === Plugin location ===
dso.plugin_root=/Users/jacobpack/.claude/plugins/cache/digital-service-orchestra/dso/1.16.11

# === Stack ===
# Project is Azure Bicep IaC — no canonical "stack" string in detect-stack.sh registry.
stack=unknown

# === Tickets ===
tickets.directory=.tickets-tracker
tickets.prefix=AKHIB

# === Checkpoint ===
checkpoint.marker_file=.checkpoint-pending-rollback

# === Merge strategy ===
merge.strategy=direct

# === Preplanning ===
preplanning.interactive=true

# === Integrations ===
design.figma_collaboration=false
confluence.enabled=false

# === Test quality ===
test_quality.enabled=true
test_quality.tool=semgrep

# === Commands ===
# commands.test — not applicable; no application test suite in this IaC repo
# Example (if added later): commands.test=bicep build main.bicep
# commands.lint — bicep build doubles as lint via compile warnings
# Example: commands.lint=bicep build main.bicep
# commands.format — relying on Bicep CLI default; no separate formatter configured
# commands.format_check — N/A
# commands.validate — Azure subscription-scope validation
# Example: commands.validate=az deployment sub validate --location <region> --template-file main.bicep --parameters params/<env>.bicepparam

# === Test gate ===
# test_gate.test_dirs — no test directories present in this repo

# === Format ===
# format.extensions — bicep
# format.source_dirs — modules,params

# === Review behavioral patterns ===
review.behavioral_patterns=modules/**/*.bicep;*.bicep;params/**/*.bicepparam;params/**/*.json

# === CI ===
# ci.workflow_name — not applicable; CI runs in Azure DevOps Pipelines, defined outside this repo
# ci.fast_gate_job — N/A
# ci.fast_fail_job — N/A
# ci.test_ceil_job — N/A
# ci.integration_workflow — N/A

# === DSO runtime defaults (manual append; values match dso-setup.sh defaults for plugin v1.16.11) ===
version=1.1.0
model.haiku=claude-haiku-4-5-20251001
model.sonnet=claude-sonnet-4-6
model.opus=claude-opus-4-6
model.provider=anthropic
model.light=claude-haiku-4-5-20251001
model.standard=claude-sonnet-4-6
model.deep=claude-opus-4-7
worktree.isolation_enabled=true
test_gate.centrality_threshold=999999
brainstorm.enforce_entry_gate=true
sprint.max_replan_cycles=2
implementation_plan.approach_resolution=autonomous
scope_drift.enabled=true
suggestion.error_threshold=10
suggestion.timeout_threshold=3
debug.max_fix_validate_cycles=3
debug.intent_search_budget=20
monitoring.tool_errors=true
clarity_check.pass_threshold=5
